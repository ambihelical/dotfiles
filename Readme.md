Introduction
------------

This is my repository used to set up new home directories. This includes "dot"
files, but also some useful configuration.

GNU stow is used to configure dotfiles using symlinks, this may require some to be moved out of
the way, but nothing will be deleted.

To update the dotfiles to the packages in packages/, run scripts/setup-dotfiles
